import pygame
from constans import *
pygame.init()                                   # it makes modules that we needed game loaded in RAM to use them in game 
game_display = pygame.display.set_mode(Size)    # makes a black window
pygame.display.set_caption(game_title)          #add a title for window

